import styled from "styled-components";

export const Title = styled.h1`
    font-size: 24px;
    font-weight: 800;
    line-height: 32px;
    color: var(--pf-gray);
    font-weight: 700;
    line-height: 24px;
`

export const SectionTitle = styled.h2`
    color: var(--pf-gray);
    font-weight: 700;
    line-height: 24px;
`